[build]
  command = "pnpm --filter @workspace/cookie-nexus-pro... run build"
  publish = "artifacts/cookie-nexus-pro/dist/public"

# Client-side routing (wouter) — serve the SPA shell for unknown paths.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
